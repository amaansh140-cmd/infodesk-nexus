import { PrismaClient } from '@prisma/client'; const p = new PrismaClient(); p.$connect().then(() => { console.log('Connected!'); process.exit(0) }).catch(e => { console.error('Failed:', e); process.exit(1) });
